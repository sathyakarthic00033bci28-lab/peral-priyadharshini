#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() 
{
    int a, b;
    scanf("%d\n%d", &a, &b);
  	  char* words[] = {"one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    // Loop through each integer n in the interval [a, b]
    for (int n = a; n <= b; n++) {
        if (n >= 1 && n <= 9) {
            // Print the lowercase English word (index is n-1)
            printf("%s\n", words[n - 1]);
        } 
        else if (n > 9 && n % 2 == 0) {
            printf("even\n");
        } 
        else if (n > 9 && n % 2 != 0) {
            printf("odd\n");
        }
    }

    return 0;
}

